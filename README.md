# 🌌 Radial Binary Search Tree (Galaxy Edition)

**Web-based visualization of Binary Search Tree (BST) data structure using Radial Layout.**
โปรแกรมจำลองการทำงานของ Binary Search Tree ในรูปแบบ "จักรวาล (Galaxy)"

## 🚀 Project Overview (ภาพรวมโปรเจกต์)
โปรเจกต์นี้พัฒนาขึ้นเพื่อแสดงการทำงานของโครงสร้างข้อมูลแบบ **Binary Search Tree (BST)** โดยฉีกกฎการนำเสนอแบบดั้งเดิม (Top-Down) มาเป็นรูปแบบ **Radial Layout (การจัดวางแบบรัศมี)** การออกแบบนี้ได้รับแรงบันดาลใจจากระบบสุริยะ (Solar System) โดยให้ **Root Node** เปรียบเสมือนดวงอาทิตย์ที่เป็นจุดศูนย์กลาง และ Node ลูกหลานจะกระจายตัวออกไปตามวงโคจร (Orbit) ซึ่งช่วยให้ประหยัดพื้นที่หน้าจอและแสดงความสัมพันธ์ของข้อมูลในมุมมองที่แตกต่างแต่ยังคงความถูกต้องตามหลักการ Computer Science 100%

## ✨ Key Features (ฟีเจอร์หลัก)
ระบบรองรับการทำงานพื้นฐาน (CRUD Operations) อย่างครบถ้วน:

* **Create (Insert):** เพิ่ม Node ใหม่ลงในระบบจักรวาล (หากค่าซ้ำระบบจะแจ้งเตือน)
* **Read (Search):** ค้นหาข้อมูลด้วยอัลกอริทึม BST เมื่อเจอ Node เป้าหมายจะมีการ **Highlight (เรืองแสง)** เพื่อแสดงตำแหน่ง
* **Delete (Remove):** ลบ Node ออกจากวงโคจร โดยระบบจะจัดการเชื่อมต่อ Node ที่เหลือให้อัตโนมัติ (Re-balancing logic)
* **Visualization:** แสดงผลกราฟิกแบบ Interactive และ Animation ที่ลื่นไหล

## 🛠 Technologies Used (เทคโนโลยีที่ใช้)
โปรเจกต์นี้เขียนด้วย **Pure Web Technologies** ไม่มีการใช้ Library ภายนอก เพื่อแสดงความเข้าใจในการเขียนโค้ดอย่างแท้จริง:
* **HTML5:** โครงสร้างหลักของหน้าเว็บ
* **CSS3:** การตกแต่ง Theme แบบ Cyberpunk/Space, Animation และ Layout
* **Vanilla JavaScript (ES6+):** เขียน Logic ของ Data Structure (BST Class) และจัดการ DOM Manipulation ทั้งหมด

## 📐 Why Radial Layout? (ทำไมต้องแบบวงกลม?)
ปกติการวาด Tree จะวาดจากบนลงล่าง (Hierarchical) แต่การใช้ **Radial Layout** มีข้อดีคือ:
1.  **Space Efficiency:** ใช้พื้นที่หน้าจอได้คุ้มค่ากว่าเมื่อปริมาณข้อมูล (Depth) เพิ่มขึ้น
2.  **Central Focus:** เน้นย้ำความสำคัญของ Root Node ที่เป็นจุดเริ่มต้นของข้อมูลทั้งหมด
3.  **Modern Aesthetics:** ให้มุมมองที่ทันสมัยและน่าสนใจกว่า Tree แบบเดิมๆ

## 🌐 Live Demo
สามารถทดลองใช้งานได้ที่นี่: [https://susunomnom.github.io/Binary-Search-Tree/]

---
**Created by:** [นางสาว ซูฮา ปัตนกุล]
**Course:** Data Structures & Algorithms
